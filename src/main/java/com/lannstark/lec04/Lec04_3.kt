package com.lannstark.lec04

/**
 * 3. 논리 연산자와 코틀린에만 있는 특이한 연산자
 */

/**
 * 논리 연산자
 * &&, ||, !
 * /// ALL SAME
 * /// Java처럼 Lazy 연산을 수행함
 */

/**
 * in / !in : 컬렉션이나 범위에 포함되어 있다, 그렇지 않다
 * a..b : a부터 b까지의 범위 객체를 생성한다
 * a[i] : 문자열 a에서 특정 Index i로 값을 가져온다
 * a[i] = b : 문자열 a에서 특정 Index i로 값을 넣는다
 */
