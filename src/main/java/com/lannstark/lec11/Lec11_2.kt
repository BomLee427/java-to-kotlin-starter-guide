package com.lannstark.lec11

/**
 * 2. 코틀린 파일의 접근 제어
 */

/**
 * .kt 파일에 변수, 함수, 클래스 여러 개를 바로 만들 수 있다.
 */

/**
 * 파일에 접근 제어 지시어를 붙이면?
 * public: 기본으로 붙어 있음. 어디서든 접근 가능해짐.
 * protected: 파일(최상단)에는 사용 불가
 * - 선정된 클래스와 하위 클래스에 작동하는 지시어이므로 클래스에만 선언 가능
 * internal: 같은 모듈에만 접근 가능
 * private: 같은 파일 내에서 접근 가능
 */
